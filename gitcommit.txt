#NEXT COMMIT MESSAGE:

Added cookies to login.php so that the script uses sessions and cookies.
Index.php now checks if a cookie is set, and if so adds the info to the session.
Cleared cookies in logout.php.